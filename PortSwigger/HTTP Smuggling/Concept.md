HTTP Smuggling is a type of vulnerability that inserts requests into the request queue, so that if the victim sends a request, it could be a hidden request that the attacker has already sent.
![[Pasted image 20260106041655.png]]
